<%= form_for(@add_fields_to_user) do |f| %>
  <% if @add_fields_to_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_fields_to_user.errors.count, "error") %> prohibited this add_fields_to_user from being saved:</h2>

      <ul>
      <% @add_fields_to_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
